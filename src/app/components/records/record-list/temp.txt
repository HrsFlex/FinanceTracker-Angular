<div class="max-w-7xl mx-auto p-6">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-bold text-sky-800">Records</h2>
    <a
      routerLink="/record-upsert"
      class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition"
    >
      + Add Record
    </a>
  </div>

  <!-- Accounts Section -->
  <div class="mb-8">
    <h3 class="text-2xl font-semibold text-sky-700 mb-4">Accounts</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
      <div
        *ngFor="let account of accounts"
        class="p-4 bg-white rounded-lg shadow border border-sky-100"
      >
        <h4 class="text-lg font-bold text-sky-800">{{ account.name }}</h4>
        <p class="text-sky-600">{{ account.description }}</p>
        <p class="text-xs text-sky-400 mt-2">
          Updated: {{ account.updatedDate | date }}
        </p>
        <h2 class="text-lg font-bold text-sky-800">
          Balance: {{ account.balance }}
        </h2>
      </div>
    </div>
  </div>

  <!-- Categories Section -->
  <div>
    <h3 class="text-2xl font-semibold text-sky-700 mb-4">Categories</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
      <div
        *ngFor="let category of categories"
        class="p-4 bg-white rounded-lg shadow border border-sky-100"
      >
        <h4 class="text-lg font-bold text-sky-800">{{ category.name }}</h4>
        <p class="text-sky-600">{{ category.description }}</p>
        <!-- <p class="text-xs text-sky-400 mt-2">Type: {{ category.type }}</p> -->
      </div>
    </div>
  </div>
</div>
{{ records | json }}
